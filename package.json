{
  "name": "fx-line-control-lab",
  "version": "1.0.0",
  "private": true,
  "description": "Bilingual FX-style PLC and HMI production-line training simulator",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "check": "npm run test && npm run build"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "vite": "^6.2.2",
    "vitest": "^3.2.4"
  }
}
